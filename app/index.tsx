import React from 'react'
import { Redirect } from 'expo-router'

const index = () => {
  return (

    // <Redirect href="(screens)/(initializations)/initialization1" />
    // <Redirect href="(screens)/login" />
  <Redirect href="(screens)/login" />
    // <Redirect href="(screens)/(previews)/preview1" />
  );
}


export default index
